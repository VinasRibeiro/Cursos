//Diferente de outras linguagems como python e ruby em que um objeto é formado por uma classe e depois instanciado
//O java script usa um sistema parecido com uma biblioteca no python e um hash no ruby para definir um objeto
//O java script considera um objeto.

let pessoa = {
    nome: 'Rafael',
    idade: 25,
    estaAprovado: true,
    sobrenome: 'de souza'
};

console.log(pessoa);